package com.mytelco.customerbff.operator;

public enum OperatorAdapterErrorCode {
    UPSTREAM_TIMEOUT,
    UPSTREAM_UNAVAILABLE,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    VALIDATION,
    INTERNAL
}
