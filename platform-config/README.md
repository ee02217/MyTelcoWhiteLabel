# Platform Configuration Module

This directory contains platform-wide configuration, shared libraries, and common utilities.

## Structure

- Placeholder for shared configuration and utilities

## Available Commands

When this module has a package.json with scripts, root-level commands will execute them:

- `npm run lint:changed` - Lint changed modules
- `npm run test:changed` - Test changed modules
- `npm run build:changed` - Build changed modules
