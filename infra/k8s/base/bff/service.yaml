apiVersion: v1
kind: Service
metadata:
  name: bff
  labels:
    app: bff
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: bff
